# --------------------------------------------------------------------------
# This approach ignores all files and directories by default, and then
# explicitly re-includes only the specific directories and files you want
# to track.
# --------------------------------------------------------------------------

# Ignore everything in the repository
*

# Exclude the .gitignore file itself from being ignored
!.gitignore

# Explicitly re-include the specific directories you want to track
# Use /** to include all files and subdirectories recursively
!fish/
!fish/**
!nvim/
!nvim/**
!ghostty/
!ghostty/**
!tmux/
!tmux/**

# Add specific ignore rules for files within the tracked directories.
# This rule will ignore any file with a .bak extension, even inside the
# directories you are tracking.
*.bak

# macOS specific files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
